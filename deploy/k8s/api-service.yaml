apiVersion: v1
kind: Service
metadata:
  name: finmate-api
  labels:
    app.kubernetes.io/name: finmate
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: finmate
    app.kubernetes.io/component: api
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
